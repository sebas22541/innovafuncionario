{
  "name": "innovafuncionario-api",
  "private": true,
  "type": "module",
  "main": "index.ts",
  "dependencies": {
    "pg": "^8.20.0"
  },
  "workspaces": [
    "backend"
  ],
  "scripts": {
    "build": "npm --workspace backend run prisma:generate && npm --workspace backend run check"
  },
  "engines": {
    "node": "22.x"
  }
}
